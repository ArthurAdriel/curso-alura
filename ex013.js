const userLogado = true
const admin = false

if (userLogado === true && admin === true) {
    console.log("Você entrou no sistema!")
} else {
    console.log("Você não tem permissão para entrar no sistema!")
}